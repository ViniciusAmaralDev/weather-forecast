import React from "react";
import { Container } from "./styles";
import Header from "@components/header";

const ForecastsLayout = () => {
  return (
    <Container header={<Header title="Favoritos" leftContainer={<></>} />}>
      <></>
    </Container>
  );
};

export default ForecastsLayout;
